import { makeStyles } from "@material-ui/core/styles";

const projectCreateStyles = makeStyles({
  floatCentre: {
    width: "fit-content",
    marginLeft: "auto",
    marginRight: "auto",
  },
});

export { projectCreateStyles };
